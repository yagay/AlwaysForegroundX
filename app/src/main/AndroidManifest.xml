<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:name="com.yagay.MiniWindowGuard.GuardApp"
        android:allowBackup="false"
        android:description="@string/app_description"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <service
            android:name="com.yagay.MiniWindowGuard.ContainerOverlayService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Persistent controls for user-managed TaskSurface mini-window containers" />
        </service>

        <receiver
            android:name="com.yagay.MiniWindowGuard.SystemEventReceiver"
            android:exported="true"
            android:permission="android.permission.MANAGE_ACTIVITY_TASKS">
            <intent-filter>
                <action android:name="com.yagay.MiniWindowGuard.action.TASK_CAPTURED" />
            </intent-filter>
        </receiver>

        <activity
            android:name="com.yagay.MiniWindowGuard.TargetAppsActivity"
            android:exported="false" />

        <activity
            android:name="com.yagay.MiniWindowGuard.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
